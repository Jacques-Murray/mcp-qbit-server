# MCP qBittorrent Server Configuration
# Copy this file to .env and update with your values

# Server Configuration
PORT=8000
NODE_ENV=development

# qBittorrent Configuration (REQUIRED)
QBIT_BASE_URL=http://localhost:8080
QBIT_USERNAME=admin
QBIT_PASSWORD=your_secure_password_here

# Request timeout in milliseconds (OPTIONAL)
# Default: 10000 (10 seconds)
QBIT_TIMEOUT=10000

# Production Notes:
# - Use strong, unique passwords for QBIT_PASSWORD
# - Never commit the .env file to version control
# - Set NODE_ENV=production in production environments
# - Ensure QBIT_BASE_URL is accessible from the server
# - Consider using a secrets management system in production
